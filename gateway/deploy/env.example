# MCP Gateway Production Environment Template

# --- Core Gateway Settings ---
PORT=3000
NODE_ENV=production
# Choose between 'sqlite' or 'postgres' (adapter will handle URL)
DATABASE_URL=sqlite://mcp_gateway.db

# --- Security ---
# Secret for JWT tool-auth (if enabled)
JWT_SECRET=super-secret-change-me

# --- Ledger & Accounting ---
# Mode for ledger unavailability: 'open' (prioritize availability) or 'closed' (prioritize integrity)
LEDGER_FAIL_MODE=closed

# --- Observability ---
# OTLP endpoint for tracing/metrics
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
OTEL_SERVICE_NAME=mcp-gateway-prod

# --- Reliability ---
CIRCUIT_BREAKER_THRESHOLD=5
RETRY_MAX_ATTEMPTS=3
RETRY_BACKOFF_MS=500
